# Artera Storage API Configuration
# Copy this file to .env and update the values as needed

# Base URL for the API (used in API documentation, responses, and web UI)
# Examples:
#   Development: http://localhost:8975
#   Production: https://your-domain.com
#   Coolify: https://your-app-name.your-domain.com
BASE_URL=http://localhost:8975

# Port number for the API server
PORT=8975

# CORS allowed origins (comma-separated list, or * for all)
# Examples:
#   Single origin: https://your-frontend.com
#   Multiple origins: https://app1.com,https://app2.com
#   All origins: *
CORS_ORIGINS=*

# Enable auto-reload for development (true/false)
# Set to false in production
RELOAD=true

# Storage root directory name (default: artera)
# This is the folder name where all files will be stored
# Examples:
#   Default: artera
#   Custom: storage, data, files
STORAGE_ROOT=artera

# JWT Authentication Configuration
# Secret key for JWT token signing (CHANGE THIS IN PRODUCTION!)
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-change-this-in-production

# JWT token expiration time in minutes (default: 1440 = 24 hours)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440
